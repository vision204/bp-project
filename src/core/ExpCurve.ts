// ---------------------------------------------------------------------------
// 레벨 곡선만 담은 작은 모듈입니다.
//
// 섬 정의(world/islands.ts)가 몬스터 경험치를 이 곡선에서 계산하고,
// GameState도 같은 곡선을 쓰기 때문에 한쪽이 다른 쪽을 import하면
// 순환 참조가 생깁니다. 그래서 곡선만 따로 떼어 양쪽이 이 파일을 보게 했습니다.
// ---------------------------------------------------------------------------

/**
 * 레벨업에 필요한 경험치. 예전에는 1.35배씩 지수 증가라 50레벨만 가도 도달 불가능한
 * 숫자가 됐습니다. 섬 해금 레벨이 최대 900까지 있으므로 다항식 곡선으로 바꿔서
 * 후반까지 현실적으로 도달할 수 있게 했습니다.
 */
export function expRequiredForLevel(level: number): number {
  return Math.round(40 + Math.pow(level, 1.6) * 6);
}

/**
 * 캐릭터 레벨 상한. 대저택(마지막 섬) 최상위 종족("저택의 주인")의 적정
 * 레벨(1900 + 50×3 = 2050)보다 여유 있게 위로 잡아, 마지막 콘텐츠까지 다
 * 겪고도 조금 더 올릴 여지를 남겨둡니다. 이 레벨에 도달하면 grantExp(Leveling.ts)가
 * 더 이상 레벨도 스탯 포인트도 올리지 않습니다.
 */
export const MAX_LEVEL = 2056;
